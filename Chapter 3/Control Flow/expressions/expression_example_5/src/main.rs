fn main() {
    // set condition to true
    let condition = true;
    // store condition return to number
    let number = if condition { 5 } else { 6 };
    // display value
    println!("The value of number is: {number}");
}
