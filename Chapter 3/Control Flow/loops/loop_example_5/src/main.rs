fn main() {
    // create an array 
    let a = [10, 20, 30, 40, 50];
    // create index 
    let mut index = 0;

    // along index is in range
    while index < 5 {
        println!("the value usL {}", a[index]);
        index += 1; // increment index 
    }
}

