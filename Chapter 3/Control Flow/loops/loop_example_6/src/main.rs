fn main() {
    // create an array
    let a = [10, 20, 30, 40, 50];
    // iterate over each element in a
    for element in a {
        // display message
        println!("the value is: {element}");
    }
}