fn main() {
    // use range from standard library
    // .rev() to reverse the count
    for number in (1..4).rev() {
        // display message
        println!("{number}!");
    }
    // display message
    println!("LIFTOFF!!!");
}
